<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.forum2.dao.BoardMapper">
  <select id="selectTopByNum" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from t_board
    where
        state=0 and deleteState=0
        order by sort asc
        limit  #{num,jdbcType=INTEGER}
  </select>
    <!-- 用户发帖数加1 -->
    <update id="upArticleCountById" parameterType="java.lang.Long">
        update t_board set articleCount=articleCount+1 where id=#{id,jdbcType=BIGINT}
    </update>

    <!-- 用户发帖数加1 -->
    <update id="downArticleCountById" parameterType="java.lang.Long">
        update t_board set articleCount=articleCount-1 where id=#{id,jdbcType=BIGINT}
    </update>




</mapper>