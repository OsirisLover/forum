<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.forum2.dao.UserMapper">

    <select id="selectByUsername" parameterType="java.lang.String" resultMap="BaseResultMap" >
  select
        <include refid="Base_Column_List" />
  from t_user
           where
               deleteState=0 and
               username=#{username,jdbcType=VARCHAR}
    </select>
    <!-- 用户发帖数加1 -->
<update id="upArticleCountById" parameterType="java.lang.Long">
    update t_user set articleCount=articleCount+1 where id=#{id,jdbcType=BIGINT}
</update>

    <update id="downArticleCountById" parameterType="java.lang.Long">
        update t_user set articleCount=articleCount-1 where id=#{id,jdbcType=BIGINT}
    </update>




</mapper>